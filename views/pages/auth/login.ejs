<!-- start tag start -->
<%- include('../../includes/start.ejs')%>
<!-- start tag end -->

<!-- signup page start -->
<div class="signup-section">
    <form class="form-signin" action="/auth/login" method="POST">
        <div class="text-center mb-4">
            <img class="mb-4" src="/images/logo.png" alt="" width="72" height="72">
            <h1 class="h3 mb-3 font-weight-normal">Floating labels</h1>
        </div>

        <div class="form-label-group">
            <input type="email" id="inputEmail" class="form-control mb-3" name="email" placeholder="Email address" value="<%= value.email ? value.email : '' %>" required autofocus>
            <label for="inputEmail">Email address</label>
        </div>

        <div class="form-label-group">
            <input type="password" id="inputPassword" class="form-control mb-3" name="password" placeholder="Password" value="<%= value.password ? value.password : '' %>" required>
            <label for="inputPassword">Password</label>
        </div>

        <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>

        <%
            if(formError.error){%>
                <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                    <strong><%= formError.error && formError.error %></strong>
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            <%}
        %>

        <%
            if(serverError.error){%>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <strong><%= serverError.error && serverError.error %></strong>
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            <%}
        %>

        <p class="text-center mt-3">don't have an account? just <a href="/auth/signup">signup</a></p>
    </form>
</div>
<!-- signup page end -->

<!-- end tag start -->
<%- include('../../includes/end.ejs')%>
<!-- end tag end -->